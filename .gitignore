# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# App Launcher local files
config.json
.agent-prompt.txt
agent-session.ps1
.superpowers/
logs/

# Release artifacts — the signed installer copy made at the repo root during
# the release script's signing step. Never committed; lives only on disk
# long enough to get uploaded as a GitHub release asset.
/app.msi
/app.msi.sig
/TakeOffR1.ico
/preview_current_32.png
